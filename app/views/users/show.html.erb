<section class="user-section">
  <h2>
    <%= "#{@user.name}" %>&nbsp;
    <small><%= "#{@user.email}" %></small>
  </h2>
  <% invite_links(@user) %>
  <article class="timeline">
    <h3>Recent posts:</h3>
    <ul class="posts">
      <%= render @posts %>
    </ul>
  </article>
</section>
